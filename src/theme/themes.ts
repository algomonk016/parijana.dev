import type { Theme } from './types'

export const themes: Record<string, Theme> = {
  teal: {
    name: 'teal',
    accent: '#64FFDA',
    label: 'Teal',
  },
  orange: {
    name: 'orange',
    accent: '#FF8C42',
    label: 'Orange',
  },
}
